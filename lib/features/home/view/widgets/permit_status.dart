import 'package:flutter/material.dart';

/// This widget displays the permit's status information. It has 3 states, [ValidPermit], [WithdrawnPermit], & [ExpiredPermit].
///
/// Only use this when the user has a permit. If the user does not have a permit then use [PermitApplicationStatus]
class PermitStatusWidget extends StatelessWidget {
  const PermitStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
