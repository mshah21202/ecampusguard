import 'package:flutter/material.dart';

class PermitApplicationStatusWidget extends StatelessWidget {
  const PermitApplicationStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
